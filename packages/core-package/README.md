# Core Package

Core business logic package in the monorepo.

## Usage

To use this package in another workspace:

```javascript
import { coreFunction } from '@monorepo/core-package';
```

## Configuration

Runs on port 3001 by default.

## Dependencies

- `@monorepo/shared` - Shared utilities and database connection
- `express` - Web framework

